# ===== AI Provider 配置示例 =====
# 复制为 .env 并填入真实值。真实 API Key 严禁提交到 Git / GitHub Pages 静态文件。
# 当前版本: 用户在应用内「设置 → AI 助手」中配置 Provider/Base URL/Key/Model,
# 数据保存在浏览器本地(localStorage), 不会进入仓库。
# 未来接入安全后端代理后, 可由环境变量注入。

# Provider: openai / deepseek / qwen / doubao / silicon / azure / custom
AI_PROVIDER=openai
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-4o-mini

# 安全代理(可选, 未来后端): 前端只调你的代理, 代理持有真实 Key
PROXY_BASE_URL=